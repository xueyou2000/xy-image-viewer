import { CanvasViewOptions } from "./interface";

/**
 * 默认配置
 */
export const DefaultOptions: CanvasViewOptions = {
    zoomOnWheel: true,
    zoomRatio: 0.07,
    zoomMin: 0.1,
    zoomMax: 2.1,
    resize: false,
    gestureRotate: false,
};
